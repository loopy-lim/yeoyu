<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
    </style>

    <style name="ExternalPipTheme" parent="AppTheme">
        <item name="android:windowDisablePreview">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>

    <!-- Extension action bubble: grow from center, quick fade out. -->
    <style name="ExtensionPopup" parent="@android:style/Animation.Dialog">
        <item name="android:windowEnterAnimation">@anim/extension_popup_in</item>
        <item name="android:windowExitAnimation">@anim/extension_popup_out</item>
    </style>

</resources>
