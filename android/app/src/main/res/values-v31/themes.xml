<resources>

    <!-- Android 12+ system splash: brand background and the launcher mark.
         Pre-31 devices keep the plain launch theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
        <item name="android:windowSplashScreenBackground">@color/yeoyu_icon_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/yeoyu_icon</item>
    </style>

</resources>
