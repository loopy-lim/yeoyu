# 이전 기본 단축키 회귀 데이터

`legacy-keymap.json`은 이전 기본 설정에서 확인한 19개 항목을 공개 회귀 fixture로 정리한 것이다. 초기 기본 15개에 Tab 순환 4개를 적용한 결과와 키, 모든 수정 키, 명령, 순서가 정확히 같다.

테스트는 이 디렉터리의 sanitize된 복사본만 사용하며 원시 기기 자료에 의존하지 않는다.

이관 범위는 확인된 19개 전체가 같은 설정으로 제한한다. 15개만 있는 설정, 항목의 추가·삭제·재정렬, 키·수정 키·명령 변경, 빈 설정은 그대로 보존한다.
